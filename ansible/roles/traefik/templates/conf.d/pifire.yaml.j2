---
http:
  routers:
    pifire:
      rule: "Host(`{{ pifire_hostname }}`)"
      entryPoints:
        - websecure
      service: pifire
      tls:
        certResolver: letsencrypt

  services:
    pifire:
      loadBalancer:
        servers:
          - url: "{{ pifire_backend_url }}"
