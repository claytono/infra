---
apiVersion: apps/v1
kind: Deployment

metadata:
  name: ara-ara

spec:
  template:
    spec:
      containers:
      - name: ara
        env:
        - name: ARA_DATABASE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: ara-postgres-app
              key: password
