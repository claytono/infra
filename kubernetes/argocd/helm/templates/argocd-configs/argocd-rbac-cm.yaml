---
# Source: argo-cd/templates/argocd-configs/argocd-rbac-cm.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-rbac-cm
  namespace: argocd
  labels:
    helm.sh/chart: argo-cd-8.1.0
    app.kubernetes.io/name: argocd-rbac-cm
    app.kubernetes.io/instance: argocd
    app.kubernetes.io/component: server
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/part-of: argocd
    app.kubernetes.io/version: "v3.0.6"
data:
  policy.csv: "# Allow GitHub Actions to read application status\np, role:github-actions,
    applications, get, *, allow\n# Allow GitHub Actions to trigger sync operations  \np,
    role:github-actions, applications, sync, *, allow\n# Allow GitHub Actions to update
    applications (needed for target revision changes)\np, role:github-actions, applications,
    update, *, allow\n# Assign github-actions account to the role\ng, github-actions,
    role:github-actions\n"
  policy.default: ""
  policy.matchMode: glob
  scopes: '[groups]'
