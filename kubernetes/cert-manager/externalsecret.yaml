---
apiVersion: external-secrets.io/v1
kind: ExternalSecret

metadata:
  name: cert-manager

spec:
  secretStoreRef:
    name: production
    kind: ClusterSecretStore
  dataFrom:
  - extract:
      key: cert-manager
