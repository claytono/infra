---
apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: cross-seed-config

spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: synology-iscsi
  resources:
    requests:
      storage: 10Gi
