---
apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: cwa-metadata
  labels:
    velero.io/backup: 'true'

spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: synology-iscsi
  resources:
    requests:
      storage: 10Gi
