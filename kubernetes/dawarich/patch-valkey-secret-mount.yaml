---
apiVersion: apps/v1
kind: Deployment

metadata:
  name: valkey

spec:
  template:
    spec:
      initContainers:
      - name: valkey-init
        volumeMounts:
        - name: extravalkeyconfigs-volume
          mountPath: /extravalkeyconfigs
      volumes:
      - name: extravalkeyconfigs-volume
        secret:
          secretName: dawarich-valkey-config
          items:
          - key: auth.conf
            path: auth.conf
