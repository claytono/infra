---
apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: docker-registry-proxy-cache

spec:
  accessModes:
  - ReadWriteOncePod
  resources:
    requests:
      storage: 50Gi

---
apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: docker-registry-proxy-ca

spec:
  accessModes:
  - ReadWriteOncePod
  resources:
    requests:
      storage: 1Gi
