---
apiVersion: external-secrets.io/v1
kind: ExternalSecret

metadata:
  name: got-your-back

spec:
  secretStoreRef:
    name: production
    kind: ClusterSecretStore
  dataFrom:
  - extract:
      key: gyb-files
  - extract:
      key: gyb-vars
