---
apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: grimmory-data
  namespace: grimmory

spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: nfs
  resources:
    requests:
      storage: 2Gi
