---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: canary-ping
spec:
  # Run hourly - canary has 50min timeout, so it goes DOWN ~10min before next ping
  # Each DOWN transition sends email to cloud healthchecks.io, verifying full stack
  schedule: "0 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      activeDeadlineSeconds: 60
      template:
        spec:
          restartPolicy: Never
          containers:
          - name: ping
            image: curlimages/curl:8.18.0@sha256:d94d07ba9e7d6de898b6d96c1a072f6f8266c687af78a74f380087a0addf5d17
            env:
            - name: CANARY_PING_KEY
              valueFrom:
                secretKeyRef:
                  name: healthchecks
                  key: CANARY_PING_KEY
            args:
            - -fv
            - -m
            - "10"
            - --retry
            - "2"
            - https://hc.k.oneill.net/ping/$(CANARY_PING_KEY)/heartbeat-canary
