---
apiVersion: batch/v1
kind: CronJob

metadata:
  name: hortusfox-tasks
  namespace: hortusfox

spec:
  schedule: 0 */4 * * *
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: OnFailure
          securityContext:
            seccompProfile:
              type: RuntimeDefault
          containers:
          - name: cronjobs
            image: curlimages/curl:8.19.0@sha256:c03110c736db81bbe1be0296f1f1608c81b954b01626bdfb0a8f84e5bd00ff3c
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities:
                drop:
                - ALL
            env:
            - name: CRONPW
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: cronpw
            command:
            - /bin/sh
            - -c
            - |-
              BASE="http://hortusfox.hortusfox.svc.cluster.local"
              failed=0
              for path in \
                /cronjob/tasks/overdue \
                /cronjob/tasks/tomorrow \
                /cronjob/tasks/recurring \
                /cronjob/calendar/reminder \
                /cronjob/backup/auto; do
                echo "Running $path"
                if ! curl -sf --connect-timeout 5 --max-time 30 "${BASE}${path}?cronpw=${CRONPW}"; then
                  echo "FAILED: $path"
                  failed=1
                fi
              done
              exit "$failed"
