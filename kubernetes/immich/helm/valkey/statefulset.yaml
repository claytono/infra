---
# Source: valkey/templates/statefulset.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: immich-valkey
  labels:
    app.kubernetes.io/name: valkey
    app.kubernetes.io/instance: immich-valkey
    app.kubernetes.io/version: "9.1.2"
    app.kubernetes.io/managed-by: Helm
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: valkey
      app.kubernetes.io/instance: immich-valkey
  template:
    metadata:
      annotations:
        checksum/customconfig: 01ba4719c80b6fe911b091a7c05124b64eeece964e09c058ef8f9805daca546b
        checksum/scripts: 71cf29409be4adf24a1bcbe05778fd5a99a57c3827d1507c75b64c00596a8d84
        reloader.stakater.com/auto: "true"
      labels:
        app.kubernetes.io/name: valkey
        app.kubernetes.io/instance: immich-valkey
    spec:
      serviceAccountName: default
      securityContext:
        fsGroup: 999
        supplementalGroups:
        - 999
      initContainers:
        - name: valkey-init
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
              - ALL
            privileged: false
            readOnlyRootFilesystem: true
            runAsGroup: 999
            runAsNonRoot: true
            runAsUser: 999
          image: "docker.io/valkey/valkey:9.0.0"
          imagePullPolicy: IfNotPresent
          envFrom:
          volumeMounts:
            - mountPath: /data
              name: valkey-data
            - name: scripts
              mountPath: /scripts
            - mountPath: /extravalkeyconfigs
              name: extravalkeyconfigs-volume
          command: [ "/scripts/init.sh" ]
      containers:
        - name: valkey-server
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
              - ALL
            privileged: false
            readOnlyRootFilesystem: true
            runAsGroup: 999
            runAsNonRoot: true
            runAsUser: 999
          image: "docker.io/valkey/valkey:9.0.0"
          imagePullPolicy: IfNotPresent
          ports:
            - name: valkey
              containerPort: 6379
              protocol: TCP
          startupProbe:
            exec:
              command:
                - sh
                - -c
                - valkey-cli ping
            initialDelaySeconds: 10
            timeoutSeconds: 5
            failureThreshold: 30
            successThreshold: 1
            periodSeconds: 10
          command:
            - valkey-server
          args:
            - /data/conf/valkey.conf
          envFrom:
          volumeMounts:
            - name: valkey-data
              mountPath: /data
      volumes:
        - name: scripts
          configMap:
            name: immich-valkey-scripts
            defaultMode: 0555
        - name: extravalkeyconfigs-volume
          secret:
            secretName: immich-valkey-config
        - name: valkey-data
          persistentVolumeClaim:
            claimName: immich-valkey

