---
apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: komga-config
  labels:
    velero.io/backup: 'true'

spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: synology-iscsi
  resources:
    requests:
      storage: 20Gi
