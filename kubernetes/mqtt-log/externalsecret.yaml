---
apiVersion: external-secrets.io/v1
kind: ExternalSecret

metadata:
  name: mqtt-log

spec:
  secretStoreRef:
    name: production
    kind: ClusterSecretStore
  dataFrom:
  - extract:
      key: mqtt-log
