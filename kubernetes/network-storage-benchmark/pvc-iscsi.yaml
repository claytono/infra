---
apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: network-storage-benchmark-iscsi

spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: synology-iscsi
  resources:
    requests:
      storage: 80Gi
