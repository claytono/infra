---
apiVersion: external-secrets.io/v1
kind: ExternalSecret

metadata:
  name: plexmediaserver

spec:
  secretStoreRef:
    name: production
    kind: ClusterSecretStore
  dataFrom:
  - extract:
      key: plexmediaserver
