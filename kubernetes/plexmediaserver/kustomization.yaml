---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

commonAnnotations:
  argoManaged: 'true'

resources:
- deploy.yaml
- ingress.yaml
- pvc.yaml
- service.yaml
- externalsecret.yaml

images:
- name: plexinc/pms-docker
  newTag: 1.42.1.10060-4e8b05daf

labels:

- pairs:
    app.kubernetes.io/name: plexmediaserver
    app.kubernetes.io/instance: plexmediaserver
  includeSelectors: true
