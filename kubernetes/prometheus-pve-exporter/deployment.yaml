---
apiVersion: apps/v1
kind: Deployment

metadata:
  name: exporter

spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: prometheus-pve-exporter
      app.kubernetes.io/instance: prometheus-pve-exporter
  template:
    metadata:
      labels:
        app.kubernetes.io/name: prometheus-pve-exporter
        app.kubernetes.io/instance: prometheus-pve-exporter
      annotations:
        reloader.stakater.com/auto: 'true'
    spec:
      volumes:
      - name: tmp
        emptyDir: {}
      containers:
      - name: pve-exporter
        image: ghcr.io/prometheus-pve/prometheus-pve-exporter:3.8.2@sha256:e3d501a82df58bc0dd0e3f1fb49f4c10d3069e71ddad68b1fbb773b65da0c177
        ports:
        - containerPort: 9221
        volumeMounts:
        - name: tmp
          mountPath: /tmp
        env:
        - name: PVE_USER
          value: pve-exporter@pve
        - name: PVE_TOKEN_NAME
          value: exporter
        envFrom:
        - secretRef:
            name: prometheus-pve-exporter
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          runAsNonRoot: true
          runAsUser: 101
          capabilities:
            drop: [ALL]
