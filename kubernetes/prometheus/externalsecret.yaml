---
apiVersion: external-secrets.io/v1
kind: ExternalSecret

metadata:
  name: prometheus

spec:
  secretStoreRef:
    name: production
    kind: ClusterSecretStore
  dataFrom:
  - extract:
      key: prometheus
