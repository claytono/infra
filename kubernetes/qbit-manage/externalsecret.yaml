---
apiVersion: external-secrets.io/v1
kind: ExternalSecret

metadata:
  name: qbit-manage

spec:
  secretStoreRef:
    name: production
    kind: ClusterSecretStore
  dataFrom:
  - extract:
      key: qbittorrent
