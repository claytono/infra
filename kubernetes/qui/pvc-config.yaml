---
apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: qui-config
  labels:
    velero.io/backup: 'true'

spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: synology-iscsi
  resources:
    requests:
      storage: 1Gi
