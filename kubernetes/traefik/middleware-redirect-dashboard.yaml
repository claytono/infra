---
apiVersion: traefik.io/v1alpha1
kind: Middleware

metadata:
  name: redirect-dashboard
  namespace: traefik

spec:
  redirectRegex:
    regex: ^https://traefik\.k\.oneill\.net/(dashboard)?$
    replacement: https://traefik.k.oneill.net/dashboard/
    permanent: true
