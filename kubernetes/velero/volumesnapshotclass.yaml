---
apiVersion: snapshot.storage.k8s.io/v1
kind: VolumeSnapshotClass

metadata:
  name: synology-iscsi-snapshots-velero
  labels:
    velero.io/csi-volumesnapshot-class: 'true'

driver: org.democratic-csi.synology-iscsi
deletionPolicy: Delete
