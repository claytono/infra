services:
  rpi-vm:
    build: .
    privileged: true  # Required for losetup
    environment:
      - IMAGE=/images/rpi.img
    volumes:
      - ${RPI_IMAGE}:/images/rpi.img:ro
    ports:
      - "2222:22"
